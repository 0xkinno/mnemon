# Domains

The second watcher uses deterministic seeded evidence for repeatability. The onchain source field is Base-shaped metadata only; no live RPC or Base transaction is claimed in this offline build.

